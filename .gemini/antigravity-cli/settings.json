{
  "allowNonWorkspaceAccess": true,
  "colorScheme": "tokyo night",
  "enableTelemetry": false,
  "model": "Gemini 3.1 Pro (High)",
  "permissions": {
    "allow": [
      "command(git branch)",
      "command(ls)",
      "command(find)",
      "command(mktemp)",
      "command(cat)",
      "command(printf)",
      "command(./test-link.sh)",
      "command(uv)",
      "command(asdf)",
      "command(mkdir)",
      "command(cd)",
      "command(bun init)",
      "command(bun add)",
      "command(bun pm)",
      "command(bun install)",
      "command(pwd)",
      "command(brew list)",
      "command(mas)",
      "command(git diff)",
      "command(git status)",
      "command(git log)",
      "command(gh)",
      "command(which)",
      "command(pandoc)",
      "command(mv)",
      "command(git add)",
      "command(git fetch)",
      "command(git merge)",
      "command(git show)",
      "command(head)",
      "command(just lint)",
      "command(just format-check)",
      "command(npm run)",
      "command(node ace test)",
      "command(git commit)"
    ]
  },
}
