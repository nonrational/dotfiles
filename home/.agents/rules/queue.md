## Task Queue

When the user prefixes a message with `/q` or `q:`, they are queuing a task for later — not asking you to start it now.

Append the task to `~/.claude/queue.md` (or note it in memory), respond with "Queued: [task]", and continue whatever you were already doing.
