---
id: disc-11
type: discrimination
status: draft
arguable: true
correct: warm
expected_rule: Default to warm and a little playful; flat and clinical is a failure mode, not a safe default.
---

Which draft follows code-review-register, and which rule decides it?

## rule

A whole review that's flat, dry, and all-business reads as cold and punitive even when every individual line is technically polite. That's a failure mode on its own, not a safe default.

## warm

nit: `doTheThing` made me laugh, but future-us will want `rebuildIndex`. Not blocking.

## flat

nit: `doTheThing` is not descriptive. `rebuildIndex` would be clearer. Not blocking.

## note

Both nits are three-part and correct. The key encodes the register's stance that flat is a failure mode, which is a taste call: a subject that prefers the flat draft for brevity has a defensible reading, so this case counts toward the floor and never hard-gates.
